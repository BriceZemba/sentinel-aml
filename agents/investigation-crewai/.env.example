# --- LLM (optional: runs deterministic crew if absent, real CrewAI crew if present) ---
ANTHROPIC_API_KEY=
SENTINEL_CREW_MODEL=anthropic/claude-opus-4-8

# --- Data source (defaults to repo /data when unset) ---
SENTINEL_DATA_DIR=

# --- UiPath (populated by `uipath auth`) ---
UIPATH_URL=
UIPATH_ACCESS_TOKEN=
