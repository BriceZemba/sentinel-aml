# --- LLM (optional: agent runs offline with a templated rationale if absent) ---
ANTHROPIC_API_KEY=
SENTINEL_LLM_MODEL=claude-opus-4-8

# --- Data source (defaults to repo /data when unset) ---
SENTINEL_DATA_DIR=

# --- UiPath (populated by `uipath auth`) ---
UIPATH_URL=
UIPATH_ACCESS_TOKEN=
